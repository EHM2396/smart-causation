# Dependencias SOLO para desarrollo y pruebas (no van a la imagen de producción).
# Instalar con:  pip install -r requirements_dev.txt
-r requirements_api.txt

pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0          # cliente para probar endpoints con TestClient de FastAPI
